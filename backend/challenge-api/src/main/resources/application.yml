# set active profile
spring:
  profiles:
    active:
    - default
    
# default
profile:
  message: This is default profile.
  
service:
  message: Hello, API!
  
server:
  port: 8082

logging:
  level:
    root: INFO
    web: DEBUG
